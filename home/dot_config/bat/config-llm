# Bat configuration for LLM agents
# Plain text output without decorations, colors, or paging

# Plain style - no line numbers, no decorations
--style=plain

# No colors
--color=never

# No paging
--paging=never

# No decorations
--decorations=never
